# HaGoKu 环境变量配置
#
# 用法：复制到用户主目录（与代码约定一致）：
#   mkdir -p ~/.hagoku && cp .env.example ~/.hagoku/.env
# 然后编辑 ~/.hagoku/.env。勿在仓库根目录放 .env（不会被 config.py 加载）。

# LLM 配置
HAGOKYU_LLM_BASE_URL=http://localhost:8080/v1
HAGOKYU_LLM_API_KEY=none
HAGOKYU_LLM_MODEL=Qwen3.6-35B-A3B

# 双层 LLM 策略（可选，不设则复用 HAGOKYU_LLM_MODEL）
# HAGOKYU_LLM_MODEL_DEEP=   # 深度推理模型（Analyst、仲裁器）
# HAGOKYU_LLM_MODEL_QUICK=  # 快速模型（Scout、Reporter、Scribe 反思）

# Embedding 配置（向量知识库）
HAGOKYU_EMBEDDING_BASE_URL=
HAGOKYU_EMBEDDING_API_KEY=
HAGOKYU_EMBEDDING_MODEL=text-embedding-3-small

# 工作目录
HAGOKYU_WORK_DIR=~/.hagoku

# （可选）项目目录覆盖
# HAGOKYU_PROJECT_DIR=
