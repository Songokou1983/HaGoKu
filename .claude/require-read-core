# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# HaGoKu 核心原则 — 改一个符号都要读
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 强制要求

**改任何代码之前（不管多小的改动），必须先读一遍这个文件确认核心。**

### 改动前必须确认

1. **最小改动**：每次只改用户要求的那一个地方，不做额外的改动
2. **改动前必须汇报**：
   - 改了什么文件
   - 改了什么内容
   - 为什么这样改
   - 改之前要征得用户同意

### 操作纪律

- 改之前 → 先汇报改动内容，用户确认后再动手
- 改之中 → 只改确认的部分，不顺手优化
- 改之后 → 汇报完成，不多做一步

---

## 互动是核心

> **Agent 的 `begin()/respond()` 是唯一入口，UI 只负责渲染 `InteractionResult.message`，不写死任何回复。**

---

### 核心规则（违反即封印 — 永远不允许）

1. **Agent 消息必须由 LLM 生成**，不是软件写死
   - Scout/Cleaner/Analyst/Reporter 的每一步回复必须是 LLM 生成的
   - UI 不能自己构造 agent 回复，不能 hardcode 回复内容

2. **每个阶段都会停下来等用户确认**
   - Scout → 停下来 → 用户确认字段
   - Cleaner → 停下来 → 用户确认清洗策略
   - Analyst → 停下来 → 用户确认分析方向
   - 不是点一下就跑到底

3. **UI 只渲染 `InteractionResult.message`**，不写死任何回复

---

## 安全工作流（防止代码丢失）

### 每次开始工作前（必须）

```bash
git add -A && git commit -m "WIP: 工作前备份 $(date +%H:%M:%S)"
```

**这条命令必须在开始任何改动之前执行**，这样改坏了可以回退。

### 每次完成一个有意义的小改动后（必须）

```bash
git add -A && git commit -m "描述你的改动"
```

**不要积累改动**，完成一小步就 commit 一次。

### 使用 git stash 暂存改动

```bash
# 暂存当前改动（不 commit）
git stash push -m "临时保存"

# 继续工作...

# 恢复暂存的改动
git stash pop
```

### 危险操作前检查

**执行 `git checkout`、`git reset`、`git rebase` 之前**，必须：
1. 检查是否有未 commit 的改动：`git status`
2. 有改动就先 commit 或 stash
3. 再执行危险操作

### 恢复误删代码

如果代码丢了，按以下顺序尝试：
1. `git reflog` — 查找所有操作历史
2. `git stash list` — 查找暂存的改动
3. `git log` — 查找最近的 commit

---

## 封印 vs 引导的区别

| | 封印（禁止） | 引导（允许） |
|--|-----------|------------|
| 位置 | 软件层（UI/业务代码） | LLM 层（prompt 指引） |
| 做法 | 写死回复内容、规定输出格式 | 描述流程、告诉 LLM 什么时机该确认什么 |
| 结果 | LLM 被限制，丧失自主性 | LLM 知道流程，自主生成 |

---

## 当前项目重点

Scout 字段理解是整个流程的基础。字段理解做好，后续 Cleaner/Analyst/Reporter 才能正常工作。

当前阶段重点：把 Scout 字段理解做扎实。
