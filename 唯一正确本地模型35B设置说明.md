# 唯一正确本地模型 35B 设置说明

## 模型信息

- **模型**：Qwen3.6-35B-A3B-Claude-4.6-Opus-Reasoning-Distilled
- **量化**：Q4_K_M（GGUF）
- **参数量**：34.6B
- **上下文**：128000（128K）
- **Embedding**：2048

## 启动命令

```bash
~/llama.cpp-build/build/bin/llama-server \
    -m ~/models/Qwen3.6-35B-A3B-Claude-4.6-Opus-Reasoning-Distilled.Q4_K_M.gguf \
    -ngl 99 -ts 62,38 \
    -c 128000 \
    --host 0.0.0.0 \
    --port 8080 \
    -t $(nproc) \
    --metrics
```

### 参数说明

| 参数 | 值 | 说明 |
|------|----|------|
| `-m` | 模型路径 | `~/models/Qwen3.6-35B-A3B-Claude-4.6-Opus-Reasoning-Distilled.Q4_K_M.gguf` |
| `-ngl 99` | 99 | 将所有层 offload 到 GPU |
| `-ts 62,38` | tensor split | RTX 3080(20GB) 分 62%，RTX 2000 Ada(16GB) 分 38% |
| `-c 128000` | 上下文窗口 | **128K**，不是 8K，不是 32K |
| `--port 8080` | 端口 | 8080（hagoku-api 占 8000） |
| `-t $(nproc)` | 线程数 | CPU 线程数 |
| `--metrics` | 指标 | 启用 metrics 端点 |

## 双卡配置

| GPU | 型号 | VRAM | tensor split | offload 显存 |
|-----|------|------|-------------|-------------|
| CUDA0 | RTX 3080 | 20GB | 62% | ~12.6 GB |
| CUDA1 | RTX 2000 Ada | 16GB | 38% | ~7.3 GB |

## HaGoKu 配置（hagokyu）

文件：`hagoku/config.py`

```python
base_url: str = "http://localhost:8080/v1"   # OpenAI 兼容 API
model: str = "Qwen3.6-35B-A3B"
api_key: str = "none"
max_tokens: int = 8192                         # 输出上限，不是上下文
```

环境变量：`~/.hagoku/.env` 或 `.env.example`

```
HAGOKYU_LLM_BASE_URL=http://localhost:8080/v1
HAGOKYU_LLM_API_KEY=none
HAGOKYU_LLM_MODEL=Qwen3.6-35B-A3B
HAGOKYU_LLM_MODEL_DEEP=Qwen3.6-35B-A3B       # 可选，Analyst 用
HAGOKYU_LLM_MODEL_QUICK=Qwen3.6-35B-A3B      # 可选，Scout/Reporter 用
```

**注意**：`config.py` 里的 `max_tokens=8192` 是**输出 token 上限**，不是模型上下文。模型上下文由 llama-server 的 `-c` 参数决定，两者互不干扰。

## 性能基准

20 轮短任务实测：**~84.5 tokens/s**（双卡 128K 上下文）

## 依赖修复记录

llama-server 依赖 `libcuda.so.1`，由 NVIDIA driver 提供。

如果启动报 `libcuda.so.1: cannot open shared object file`，安装对应版本：

```bash
sudo apt-get install libnvidia-compute-595
```

驱动版本需与 CUDA runtime 匹配（当前驱动 595.58.03）。

## 常见问题

- **端口被占**：8000 是 hagoku-api，llama-server 用 8080
- **单卡 vs 双卡**：不加 `-ts` 参数则单卡运行；双卡必须加 `-ts 62,38`
- **上下文大小**：始终用 `-c 128000`，不要改成 8192 或其他值
- **nvidia-smi 找不到**：驱动在跑但 nvidia-smi 二进制未装入 PATH，不影响 llama-server
- **禁止加 `--parallel` 和 `--cont-batching`**：这两个参数会把每路序列长度压到 32K，导致 Hermes 压缩模型检测到错误上下文窗口
